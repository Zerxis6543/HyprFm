[package]
name    = "stdb-fivem-game"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
spacetimedb = { version = "2.0" }
serde_json  = "1"
log         = "0.4"
stdb-core   = { path = "../../core/spacetimedb" }