// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void CreateCharacterHandler(ReducerEventContext ctx, string steamHex, uint slotIndex, string name, string gender);
        public event CreateCharacterHandler? OnCreateCharacter;

        public void CreateCharacter(string steamHex, uint slotIndex, string name, string gender)
        {
            conn.InternalCallReducer(new Reducer.CreateCharacter(steamHex, slotIndex, name, gender));
        }

        public bool InvokeCreateCharacter(ReducerEventContext ctx, Reducer.CreateCharacter args)
        {
            if (OnCreateCharacter == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnCreateCharacter(
                ctx,
                args.SteamHex,
                args.SlotIndex,
                args.Name,
                args.Gender
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class CreateCharacter : Reducer, IReducerArgs
        {
            [DataMember(Name = "steam_hex")]
            public string SteamHex;
            [DataMember(Name = "slot_index")]
            public uint SlotIndex;
            [DataMember(Name = "name")]
            public string Name;
            [DataMember(Name = "gender")]
            public string Gender;

            public CreateCharacter(
                string SteamHex,
                uint SlotIndex,
                string Name,
                string Gender
            )
            {
                this.SteamHex = SteamHex;
                this.SlotIndex = SlotIndex;
                this.Name = Name;
                this.Gender = Gender;
            }

            public CreateCharacter()
            {
                this.SteamHex = "";
                this.Name = "";
                this.Gender = "";
            }

            string IReducerArgs.ReducerName => "create_character";
        }
    }
}
