// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void GiveItemToIdentityHandler(ReducerEventContext ctx, string ownerIdentityHex, string itemId, uint quantity, string metadata);
        public event GiveItemToIdentityHandler? OnGiveItemToIdentity;

        public void GiveItemToIdentity(string ownerIdentityHex, string itemId, uint quantity, string metadata)
        {
            conn.InternalCallReducer(new Reducer.GiveItemToIdentity(ownerIdentityHex, itemId, quantity, metadata));
        }

        public bool InvokeGiveItemToIdentity(ReducerEventContext ctx, Reducer.GiveItemToIdentity args)
        {
            if (OnGiveItemToIdentity == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnGiveItemToIdentity(
                ctx,
                args.OwnerIdentityHex,
                args.ItemId,
                args.Quantity,
                args.Metadata
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class GiveItemToIdentity : Reducer, IReducerArgs
        {
            [DataMember(Name = "owner_identity_hex")]
            public string OwnerIdentityHex;
            [DataMember(Name = "item_id")]
            public string ItemId;
            [DataMember(Name = "quantity")]
            public uint Quantity;
            [DataMember(Name = "metadata")]
            public string Metadata;

            public GiveItemToIdentity(
                string OwnerIdentityHex,
                string ItemId,
                uint Quantity,
                string Metadata
            )
            {
                this.OwnerIdentityHex = OwnerIdentityHex;
                this.ItemId = ItemId;
                this.Quantity = Quantity;
                this.Metadata = Metadata;
            }

            public GiveItemToIdentity()
            {
                this.OwnerIdentityHex = "";
                this.ItemId = "";
                this.Metadata = "";
            }

            string IReducerArgs.ReducerName => "give_item_to_identity";
        }
    }
}
