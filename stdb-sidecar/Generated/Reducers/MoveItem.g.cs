// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void MoveItemHandler(ReducerEventContext ctx, ulong slotId, uint newSlotIndex);
        public event MoveItemHandler? OnMoveItem;

        public void MoveItem(ulong slotId, uint newSlotIndex)
        {
            conn.InternalCallReducer(new Reducer.MoveItem(slotId, newSlotIndex));
        }

        public bool InvokeMoveItem(ReducerEventContext ctx, Reducer.MoveItem args)
        {
            if (OnMoveItem == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnMoveItem(
                ctx,
                args.SlotId,
                args.NewSlotIndex
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class MoveItem : Reducer, IReducerArgs
        {
            [DataMember(Name = "slot_id")]
            public ulong SlotId;
            [DataMember(Name = "new_slot_index")]
            public uint NewSlotIndex;

            public MoveItem(
                ulong SlotId,
                uint NewSlotIndex
            )
            {
                this.SlotId = SlotId;
                this.NewSlotIndex = NewSlotIndex;
            }

            public MoveItem()
            {
            }

            string IReducerArgs.ReducerName => "move_item";
        }
    }
}
